package com.tongsr.kaleido.gradle.dsl;

public enum KaleidoProtectionDimension {
    REACHABILITY,
    ORIGINAL_IDENTITY,
    DESCRIPTOR_CLOSURE,
    RUNTIME_ATTRIBUTES,
    RESOURCE_NAME,
    PACKAGED_PATH
}
