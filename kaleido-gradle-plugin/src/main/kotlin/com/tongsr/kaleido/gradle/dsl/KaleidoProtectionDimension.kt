package com.tongsr.kaleido.gradle.dsl

enum class KaleidoProtectionDimension {
    REACHABILITY,
    ORIGINAL_IDENTITY,
    DESCRIPTOR_CLOSURE,
    RUNTIME_ATTRIBUTES,
    RESOURCE_NAME,
    PACKAGED_PATH,
}
